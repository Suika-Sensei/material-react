// Material Symbols Icons Styles
// Import Material Symbols variable fonts
import "./fonts.css";

/**
 * Import this in your app to load Material Symbols icon fonts
 *
 * @example
 * ```tsx
 * import { Styles } from '@material-react/web';
 * // Styles are automatically loaded
 * ```
 */
export const Styles = {
  /** Path to the fonts CSS file */
  fonts: "./fonts.css",
} as const;

export default Styles;
